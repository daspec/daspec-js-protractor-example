# da-spec-experiment

Example project running DaSpec specs through Protractor

## Build & development

1. Run `grunt serve`
2. Run `npm test`

## Install notes

    gem install compass
    gem install sass
